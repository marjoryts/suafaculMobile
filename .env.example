# Opcional. Sem isso, o app descobre sozinho o IP do PC pelo Expo (mesma rede Wi-Fi).
# Use quando o backend estiver em outro lugar (servidor, tunnel, ngrok...):
# EXPO_PUBLIC_API_URL=http://192.168.0.10:5000
